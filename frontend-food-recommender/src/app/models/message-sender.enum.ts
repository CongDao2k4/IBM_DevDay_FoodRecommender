/**
 * Enum representing the sender of a chat message
 */
export enum MessageSender {
  /**
   * Message sent by the user
   */
  USER = 'USER',

  /**
   * Message sent by the AI agent
   */
  AGENT = 'AGENT'
}

// Made with Bob
